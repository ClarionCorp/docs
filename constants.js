// A basic file that we use for importing variables from one spot.
// API url changed? No problem. Change it here, and it'll reflect everywhere.

const constants = Object.freeze({
  "siteUrl": "https://api.clarioncorp.net",
});

export default constants;